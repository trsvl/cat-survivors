using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CurrentWeapons : MonoBehaviour
{
    //public GameObject imagePrefab; // Drag your image prefab to this field in the inspector
    //public Transform canvas; // Drag your canvas to this field in the inspector

    //private void Start()
    //{
    //    // Make sure to assign the button's click event to the OnButtonClick method
    //    Button button = GetComponent<Button>();
    //    if (button != null)
    //    {
    //        button.onClick.AddListener(OnButtonClick);
    //    }
    //    else
    //    {
    //        Debug.LogError("No Button component found on the GameObject.");
    //    }
    //}

    //private void OnButtonClick()
    //{
    //    // Instantiate the image prefab and set its position on the canvas
    //    GameObject newImage = Instantiate(imagePrefab, GetRandomPosition(), Quaternion.identity, canvas);
    //}

    //private Vector3 GetRandomPosition()
    //{
    //    // You can modify this method to get specific XY coordinates
    //    float randomX = Random.Range(-canvas.rect.width / 2f, canvas.rect.width / 2f);
    //    float randomY = Random.Range(-canvas.rect.height / 2f, canvas.rect.height / 2f);

    //    return new Vector3(randomX, randomY, 0f);
    //}
}
